# ============================================================
# Data Mesh Hub — Databricks Asset Bundle
# ============================================================
# Catalog and schemas created by setup notebook (00_Data_Mesh_Overview_and_Setup)

bundle:
  name: data-mesh-demo
  engine: direct

variables:
  warehouse_id:
    description: "SQL Warehouse ID for dashboard and alert"
    default: "73c6e5a2e0c009e2"

include:
  - resources/*.yml

targets:
  dev:
    mode: development
    default: true
    workspace:
      host: https://adb-7405616457611961.1.azuredatabricks.net

  prod:
    mode: production
    workspace:
      host: https://adb-7405616457611961.1.azuredatabricks.net
    permissions:
      - user_name: skarotirajashekar@godevsuite060.onmicrosoft.com
        level: CAN_MANAGE
